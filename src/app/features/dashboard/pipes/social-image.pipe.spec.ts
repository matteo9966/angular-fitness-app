import { SocialImagePipe } from './social-image.pipe';

describe('SocialImagePipe', () => {
  it('create an instance', () => {
    const pipe = new SocialImagePipe();
    expect(pipe).toBeTruthy();
  });
});
