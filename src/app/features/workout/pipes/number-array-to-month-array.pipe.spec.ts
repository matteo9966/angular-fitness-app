import { NumberArrayToMonthArrayPipe } from './number-array-to-month-array.pipe';

describe('NumberArrayToMonthArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberArrayToMonthArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
