import { OptionsDirectiveDirective } from './options-directive.directive';

describe('OptionsDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new OptionsDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
