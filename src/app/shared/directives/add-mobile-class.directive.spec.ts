import { AddMobileClassDirective } from './add-mobile-class.directive';

describe('AddMobileClassDirective', () => {
  it('should create an instance', () => {
    const directive = new AddMobileClassDirective();
    expect(directive).toBeTruthy();
  });
});
